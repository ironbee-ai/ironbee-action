name: Build

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate action.yml
        run: |
          # Check action.yml exists and is valid YAML
          python3 -c "
          import yaml, sys
          with open('action.yml') as f:
              data = yaml.safe_load(f)
          assert 'name' in data, 'Missing name'
          assert 'inputs' in data, 'Missing inputs'
          assert 'runs' in data, 'Missing runs'
          assert data['runs']['using'] == 'composite', 'Not a composite action'
          print('action.yml is valid')
          "

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # The decision logic the action runs on. A broken module here would
      # otherwise fail in a customer's workflow rather than in ours.
      - name: Run the script tests
        run: node --test tests/*.test.js
